import styled from 'styled-components';

const Menu = styled.ul`
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 2rem; 
    align-items: center; 
    margin-top: 5rem;
`;

export default Menu;
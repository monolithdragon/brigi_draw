import styled from 'styled-components';

const Hero = styled.img`
  object-fit: fill;
  min-height: 8rem;
  display: flex;
  height: 100%;
  justify-content: center;
  align-items: center;
`;

export default Hero;
